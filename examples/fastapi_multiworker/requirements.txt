# FlexiAI Multi-Worker FastAPI Example - Requirements

# Core dependencies
fastapi>=0.120.0
uvicorn[standard]>=0.38.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# FlexiAI (install from local package or PyPI)
# For local development:
# pip install -e ../..
# For production:
# flexiai>=0.5.0

# Redis for state synchronization
redis>=5.0.0

# LLM Provider SDKs
openai>=1.0.0
anthropic>=0.40.0

# Optional: For production deployments
python-dotenv>=1.0.0  # Environment variable management
gunicorn>=21.2.0  # Alternative WSGI server (if needed)

# Optional: For monitoring and observability
prometheus-client>=0.19.0  # Prometheus metrics export
structlog>=23.0.0  # Structured logging
